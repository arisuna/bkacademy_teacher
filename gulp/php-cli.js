const gulp = require('gulp');
const spawn = require('child_process').spawn;
const log = require('color-log');
const argv = require('yargs').argv;

gulp.task('php-cli', function(done) {
    return done();
});
